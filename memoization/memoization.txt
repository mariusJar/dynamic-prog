________Part 1 - Memoization________________________

Memoization Recepie

1. make it work
	visualize the problem as a tree
	implement the tree using recursion
	test it

2. make it efficiant
	add memo object
	add a base case to return memo value
	store return values into a memo